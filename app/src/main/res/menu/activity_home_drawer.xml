<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/timeline"
            android:icon="@drawable/timeline"
            android:title="Timeline" />
        <item
            android:id="@+id/search"
            android:icon="@drawable/search"
            android:title="Search" />
        </group>

    <item android:title="Start your bot!">
        <menu>
            <item
                android:id="@+id/authorize"
                android:icon="@drawable/authorize"
                android:title="Enter your keys" />
            <item
                android:id="@+id/automateDMtoFollowers"
                android:icon="@drawable/message"
                android:title="Greet your followers" />
        </menu>
    </item>

</menu>
